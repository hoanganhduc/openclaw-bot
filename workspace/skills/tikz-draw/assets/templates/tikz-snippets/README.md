# Starter Snippets

These starter snippets are phase 0/1 assets for the semantic-verifier rollout.

Purpose:

- give Codex and Claude the same starter-family asset set
- seed future fixture corpora and mutation suites
- avoid blank-canvas generation as the only workflow

Current files:

- `flowchart-basic.standalone.tex`
- `dag-basic.standalone.tex`
- `tree-basic.standalone.tex`
- `commutative-basic.standalone.tex`

These are starter assets, not approval fixtures.
